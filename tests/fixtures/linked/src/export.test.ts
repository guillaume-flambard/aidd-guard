import { describe, it } from 'vitest';

describe('export', () => {
  it('writes the export as JSON', () => {});
  it('refuses an anonymous visitor', () => {});
  it.skip('streams rows instead of buffering them for a large account', () => {});
});
